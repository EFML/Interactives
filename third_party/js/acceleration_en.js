// Bewegung mit konstanter Beschleunigung, englische Texte
// Letzte Änderung 11.10.2014

var text01 = ["Start", "Pause", "Resume"];                 // Schaltknopf Start/Pause/Weiter
var text02 = "(in s)";                                     // Einheitenangabe für Zeit-Achse
var text03 = "(in m)";                                     // Einheitenangabe für Weg-Achse
var text04 = "(in m/s)";                                   // Einheitenangabe für Geschwindigkeits-Achse
var text05 = "(in m/s\u00b2)";                             // Einheitenangabe für Beschleunigungs-Achse

// Symbole:

var decimalSeparator = ".";                                // Dezimaltrennzeichen (Komma statt Punkt)
var symbolTime = "t";                                      // Symbol für Zeit
var symbolPosition = "x";                                  // Symbol für Ortskoordinate
var second = "s";                                          // Abkürzung für Sekunde
var meter = "m";                                           // Abkürzung für Meter
var meterPerSecond = "m/s";                                // Abkürzung für Meter pro Sekunde
var meterPerSecond2 = "m/s\u00b2";                         // Abkürzung für Meter pro Sekunde hoch 2
